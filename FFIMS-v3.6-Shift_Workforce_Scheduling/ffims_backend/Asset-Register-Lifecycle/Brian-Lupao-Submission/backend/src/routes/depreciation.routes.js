import express from 'express'; import { getAllDepreciation, getAssetDepreciation, calculateDepreciation, getDepreciationSummary, generateDepreciationReport } from '../controllers/depreciation.controller.js'; const router = express.Router(); router.get('/', getAllDepreciation); router.get('/summary', getDepreciationSummary); router.get('/report', generateDepreciationReport); router.post('/calculate', calculateDepreciation); router.get('/:assetId', getAssetDepreciation); export default router;

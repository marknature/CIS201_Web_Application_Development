import express from 'express'; import { getAssets, createAsset, getAssetById, updateAsset, deleteAsset, getAssetSummary } from '../controllers/assets.controller.js'; const router = express.Router(); router.get('/', getAssets); router.get('/summary', getAssetSummary); router.post('/', createAsset); router.get('/:id', getAssetById); router.put('/:id', updateAsset); router.delete('/:id', deleteAsset); export default router;

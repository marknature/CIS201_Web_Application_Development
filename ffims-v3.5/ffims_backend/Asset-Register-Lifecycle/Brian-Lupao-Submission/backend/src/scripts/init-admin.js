import mongoose from 'mongoose'; import bcrypt from 'bcryptjs'; import dotenv from 'dotenv'; import User from '../models/User.js'; dotenv.config(); const uri = process.env.MONGODB_URI || 'mongodb://localhost:27017/ffims'; const run = async () => { await mongoose.connect(uri,{ useNewUrlParser:true,useUnifiedTopology:true }); const existing = await User.findOne({email:'admin@ffims.com'}); if(existing){ console.log('Admin already exists'); process.exit(0); } const password = await bcrypt.hash('Admin@123456', Number(process.env.BCRYPT_ROUNDS || 10)); await User.create({ firstName:'Admin', lastName:'User', email:'admin@ffims.com', password, role:'admin', isActive:true, emailVerified:true }); console.log('Admin created'); process.exit(0); }; run().catch(err=>{ console.error(err); process.exit(1); });
